function saludo(nombre, mensaje) {
    console.log(mensaje + ", " + nombre + "!");
    }
 
 function saludoDespedida(nombre, mensaje) {
    console.log(mensaje + ", " + nombre + "!");
    }
    
function saludoDespedida(nombre, mensaje) {
   console.log(mensaje + ", " + nombre + "!");
   }